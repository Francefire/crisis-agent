# Python 3.12 — install into a venv (see README).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- data / analysis ---
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0
openpyxl==3.1.5          # read Dataset/*.xlsx
matplotlib==3.11.0       # analysis figures
python-pptx==1.0.2       # build .pptx decks
PyYAML==6.0.3            # agent/config.yaml
python-dotenv==1.2.2     # load .env

# --- agent (LangChain + providers) ---
langchain==1.3.11
langgraph==1.2.7
langchain-core==1.4.8
pydantic==2.13.4
# LLM providers — only the one matching LLM_PROVIDER in .env is required,
# but all three are pinned so any provider works out of the box.
langchain-google-genai==4.2.6
langchain-groq==1.1.3
langchain-mistralai==1.1.5
